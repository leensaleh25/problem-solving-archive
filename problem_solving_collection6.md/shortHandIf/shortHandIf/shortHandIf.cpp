#include <iostream>
using namespace std;

int main()
{
	int number=3;
	/*(number >= 0) ? cout<<"positive" : cout<<"negative";*/
	cout<<("\n");
	(number==0)?cout<<"zero":((number >= 0) ? cout << "positive" : cout << "negative");
}
  
